import { DownscaledImagePipe } from './downscaled-image.pipe';

describe('DownscaledImagePipe', () => {
  it('create an instance', () => {
    const pipe = new DownscaledImagePipe();
    expect(pipe).toBeTruthy();
  });
});
