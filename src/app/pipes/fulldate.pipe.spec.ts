import { FulldatePipe } from './fulldate.pipe';

describe('FulldatePipe', () => {
    it('create an instance', () => {
        const pipe = new FulldatePipe();
        expect(pipe).toBeTruthy();
    });
});
